var = numero;
var luku
taulu =['+', '-', '/', '*', '%'];
for(var i<0, i<numero.lenght, i++);
{
  for(var k=0; k<taulu.lenght, k++);
}
{
  if(????=taulu[k])
}
{
  indeksi = i
}
lisaa(luku)
{
  numero += luku;
}


function laske()
{
  numero = eval(numero);
  numero.search('%');
jono = numero.split('%')
}
